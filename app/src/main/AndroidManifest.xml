<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Features opcionales -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practica_tema1">

        <!-- Pantalla principal -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Ajustes -->
        <activity android:name=".ConfActivity" android:exported="false" />

        <!-- Llamada de emergencia -->
        <activity android:name=".llamada" android:exported="false" />

        <!-- Actividad de Dados -->
        <activity android:name=".DadosActivity" android:exported="false" />

        <!-- Actividad de Chistes -->
        <activity android:name=".ChistesActivity" android:exported="false" />
        <activity
            android:name=".acertaste"
            android:exported="false" />


    </application>

</manifest>
